{{!-- just a basic layout of plant page, once displayed we can style it more --}}
<!--save button-->
  <button class="save-button" id="save" data-id={{plant.id}}>Save</button>



{{!-- <figure class = "image is-4by3">
    <img src="/images/{{plant.fileName}}">
</figure>
<div class="box">
    <h1>{{plant.common-name}}</h1>
    <h2>{{plant.latin-name}}</h2>
</div>

<div class="box">
    <p>{{plant.description}}</p>
</div> --}}

<div class="card plantCard">
  <div class="card-image">
    <figure class="image">
      <img src="/images/{{plant.fileName}}" alt="Plant image">
    </figure>
  </div>
    <div class="card-content">
        <div class="media-content">
            <p class="title is-4">Common name: {{plant.common_name}}</p>
            <p class="subtitle is-6">Latin name: {{plant.latin_name}}</p>
            <br>
        </div>
        <div class="content">
            {{plant.description}}
        <br>
        </div>
    </div>
</div>

<div class="box">
    <p>This plant reaches maturity in {{plant.season}}.</p>
</div>


<script src="/js/plant.js"></script>